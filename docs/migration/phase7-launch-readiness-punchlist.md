# Phase 7 — launch-readiness punch-list

Living punch-list from the launch-readiness audit (issue [#102], epic [#67]).
Generated by a 35-page content/media/SEO diff against the live Webflow source
(`mapsnational.webflow.io`), each finding adversarially re-verified before
landing here. Nothing gets fixed off this list without a decision recorded
next to it — see the phase6 punch-list (`phase6-review-punchlist.md`) for the
prior round's format this one follows.

**Status:** `[ ]` open · `[x]` done · `[~]` in progress
**Type:** AFK (mechanical) · HITL (needs a decision)

## Coverage

- Content/media/SEO: all 35 assembled pages audited via the Payload API vs.
  the live Webflow page (or marked `no_live_equivalent` for net-new pages).
  One candidate finding (home page "missing Upcoming Events section") was
  rejected on adversarial re-check — it was category badges on existing
  cards, not a missing section.
- Media: **clean** — zero Webflow CDN references (`webflow.io` /
  `website-files.com` / `webflowusercontent.com`) found in any resolved media
  URL across all 35 pages. Satisfies the AC #2 "no rendered asset URLs point
  at the Webflow CDN" criterion outright.
- Responsive: **sampled, not exhaustive.** Phase 6 already did a full visual
  pass; this round manually spot-checked mobile/tablet/desktop on the areas
  touched by this week's commits (post-gallery slider, mobile hamburger nav,
  footer CTA, the private-sector-engagement slug move) and found nothing
  broken. The other ~30 pages were not re-screenshotted at every breakpoint.
- Gated `/members/*` pages: content was audited via the public Payload API
  (bypasses the Outseta route gate), but a real logged-in visual/interactive
  pass was **not** done — needs a human with an Outseta account.

---

## Critical

- [x] **LR1 — `/latest-updates` silently capped to 3 of 78 posts, no pagination** _(done, [ArchiveBlock/Component.tsx:18](../../src/blocks/ArchiveBlock/Component.tsx:18))_ → `const limit = limitFromProps || 3` coerced the seed's intentional `limit: 0` (Payload's own "no limit" convention) to a hardcoded 3, since JS treats `0` as falsy. Root-caused: `?? 10` (only the field's own default of 10 applies when limit is genuinely unset). Verified all 78 posts render. _(AFK — one-line fix, no content decision)_

## High — content gaps needing editorial input before fixing

- [x] **LR2 — `/about-us/partners` missing 3 logos + inconsistent sizing/contrast** _(done)_ → re-hosted Sahara, De Lune Corp, and Myna from the live CDN via `rehost:images` (`scripts/rehost-images.ts` REMOTE_LOGOS) and wired into `LOGO_FILENAMES` (`scripts/seed-pages.ts`), same pattern as the prior punch-list's [P1]. Grid now shows 34 logos (was 31). Also fixed two related visual bugs found while doing this, both in [LogoStrip/Component.tsx](../../src/blocks/LogoStrip/Component.tsx): (1) logos were constrained to a fixed **height** only (`w-auto`), so a wide wordmark and a square/tall mark rendered at very different visual sizes — changed to a fixed **box** (`h-12 w-28 md:h-14 md:w-32`) with `fill` + `object-contain` so every logo occupies the same footprint regardless of source aspect ratio; (2) real partner logos are a grab-bag of raw exports (some carry their own white background rectangle, some are dark-on-transparent, e.g. MLSA), which read badly floating directly on the dark theme's near-black page — added a fixed white card (`bg-[var(--neutral-white)]`, rounded, bordered, shadowed) behind every logo regardless of theme, the same theme-invariant-white pattern `PostHero` already uses for its preview card. Verified at desktop + mobile, both themes: all 34 logos legible and uniformly sized. A third issue surfaced once boxes were pixel-equal: several source files carry their own baked-in whitespace/transparent padding in wildly different amounts (e.g. ISPU's canvas was ~35% padding), so same-size boxes still read as different visual "ink" sizes. Fixed with a one-off script ([trim-logo-padding.ts](../../scripts/trim-logo-padding.ts)) that runs every partner logo through `sharp().trim()` (already a project dependency) and re-uploads the trimmed result into the same Media doc id — 29 of 34 files were meaningfully cropped. Re-verified at desktop, both themes: logos now carry consistent visual weight, no more oversized margins on some cards vs. tight crops on others. _(AFK — precedent via `rehost:images`; all three fixes were mechanical, no design judgment calls)_
- [x] **LR3 — `/about-us/mission` dropped Quran/Hadith citations on values 2-4** _(done)_ → transcribed all 7 citations verbatim from `migration/_extracted/about-us/mission.html` into `scripts/seed-pages.ts`'s `missionSlice` as proper Lexical blockquote nodes (new `blockquote`/`italic` helpers, since the existing `quote` helper builds a different block type): value 1's Hadith regained its Al-Tabarani attribution (paragraph reverted to the live wording, citation moved to its own blockquote instead of an inline paraphrase); values 2-4 regained their 6 missing Quran/Hadith blockquotes. One deliberate deviation from a literal byte-for-byte copy: dropped a stray trailing `&quot;` artifact after two of the live citations (an evident Webflow copy-paste typo, not intentional punctuation) — flagging here since this is content, not code, in case that reading is wrong. Verified all 7 citations render correctly (including inline-italic "Hadith") on `/about-us/mission#values`. _(HITL transcription — done; flag if the stray-quote call needs revisiting)_
- [x] **LR4 — `/about-us/faq` lost its 6 grouping subheadings** _(done)_ → added an optional `group` text field to the FAQ block's `items` array (`src/blocks/FAQ/config.ts`); `Component.tsx` renders a `.type-h4` subheading whenever an item's group differs from the previous item's, so a flat list (all 7 other pages using the FAQ block, `group` left blank) renders unchanged. `about-us/faq`'s 17 questions in `scripts/seed-pages.ts` tagged with their live groups (Membership 7, Privacy 3, Chapters 2, Programming 1, Scope & Focus 1, Funding 3). Chose the field-on-existing-block route over a per-page workaround since the array-of-questions shape didn't need to change, just one label per item. Verified all 6 headings render in order with the right questions under each, and the 7 other FAQ-block pages (e.g. `/programs/community-building`) still render as a flat list. _(schema decision made: field, not workaround)_
- [x] **LR5 — `/about-us/faq` missing closing "Still have questions?" section** _(done)_ → added a `cta` block after the FAQ block in `scripts/seed-pages.ts`, matching the pattern already used elsewhere on the page (same `outline` Contact button as the FAQ header). Linked Programs → `/programs`, Leadership Team → `/about-us/board-leadership`, Donation → `/donate`, Partnerships → `/about-us/partners` (all real, verified-existing pages); Contact button → `/contact`. One deviation from a literal port: left "Resources" as plain text instead of a link — the live source's own href was an unresolved Webflow placeholder (`href="#"`, same as 3 of the 4 other links before I resolved them), and unlike those, there's no `/resources` hub page anywhere in the site (header nav treats "Resources" as a dropdown of sub-pages only, and the footer's own nav omits a Resources link for the same reason) — inventing a fake destination seemed worse than leaving it unlinked. Verified all 4 real links resolve and render correctly in preview. _(AFK — schema was already in place via the existing `cta` block, no new block needed)_
- [x] **LR6 — `/resources/.../young-professionals` lost ~35 of ~37 outbound program links** _(done)_ → the URLs weren't actually gone — a prior migration pass had preserved every one as a `// Name — URL` code comment sitting directly above an unlinked `paragraph('Name')` call in `fellowshipsYoungSlice` (`scripts/seed-pages.ts`), just never wired in. Added a `progLink(label, url)` helper (a paragraph containing a single link node, opened in a new tab since every target is an external site) and converted all ~32 remaining unlinked program names across the page's 6 FAQ categories. Also dropped two redundant "Learn More / Apply: URL" trailing-text mentions from the first category's body copy, now that the program names themselves link out. Verified in preview: 32 program links now resolve to real external `href`s (plus the page's 4 pre-existing social icon links), and no leftover unconverted comment/URL pairs remain in the source. _(AFK — mechanical, URLs were already present in-source as comments)_
- [x] **LR7 — `/resources/.../mid-career-to-senior-professionals` has live TODO placeholders in production copy** _(done)_ → turned out not to need external sourcing after all — the full list was sitting right there in the gitignored Webflow export (`migration/_extracted/resources/public-service-fellowships-mid-career-to-senior-professionals.html`), just never carried over. The "Leadership & Public Administration" placeholder was stale: that category already had all 8 live entries, so it was simply removed. The "International Affairs / National Security" placeholder was real: only 10 of the live page's 41 entries had been ported; restored the other 31 (Emissary Program, Military Fellows, McCain Global Leaders, USGLC, Presidential Leadership Scholars, CXO Fellows, New America, Wilson Center FPFP, Truman Project, and 22 more), paraphrased to the same "Name — description" style the page's other entries already use. Follow-up caught in review: every entry on this page, not just the 31 restored here, had also lost the bold-linked-name treatment the live source uses on every program (`<a><strong>Name</strong></a>`) — added a `fellowship()` helper (bold name, external link, new tab, matching the LR6 outbound-link pattern) and converted all 55 entries across all three categories, not just the newly restored ones. Verified in preview: no TODO text remains, all 55 program names render bold and linked to their real external destinations. _(AFK once the source was located — no actual sourcing decision was needed)_
- [x] **LR8 — `/members/new-york-state` hero drops a sentence + link** _(done)_ → appended "Meet our Leadership team here or email us below." to the hero paragraph in `newYorkStateSlice` (`scripts/seed-pages.ts`), "here" bold and linked to `/about-us/state-committees` (a real, existing page). Also silently restored "represent" (the live wording), since local had drifted to "represents" and broke the sentence's parallel-infinitive grammar ("helps organize... and represent..."). One deviation: the live anchor is marked `target="_blank"` even though it's an internal, same-site link — every other internal cross-link in this file opens in the same tab (new-tab is reserved for external destinations, per LR6/LR7), so kept it same-tab rather than propagate what reads like a Webflow authoring slip. Verified via the Payload API: paragraph ends with the bold `here` link (`newTab: false`) pointing at `/about-us/state-committees`, followed by " or email us below." _(AFK — restore the sentence + link, target page already exists)_
- [x] **LR9 — `/members/new-york-state` missing "Supporting MAPS in New York" intro section** _(done)_ → wired the existing (previously `enableHeader: false`, all-null) cardGrid header fields in `newYorkStateSlice` with the live copy: eyebrow "Grow", heading "Supporting MAPS in New York", the two-paragraph intro (the live `<p>`'s `<br><br>` split into two Lexical paragraphs), and `anchorId: 'main-section'` matching the live heading's `id="main-section"`. Verified via the Payload API: header now renders `enableHeader: true` with both paragraphs and the anchor id intact. _(AFK — the source copy exists in `migration/_extracted/members/new-york-state.html`, just wire it into the existing header fields)_

## Medium

- [x] **LR10 — `/programs/community-building` "Contact" buttons are dead `#` links** _(done)_ → both the hero CTA and the FAQ-header CTA share one `contactLink` object in `communityBuildingSlice` (`scripts/seed-pages.ts`), so a single `url: '#'` → `url: '/contact'` fix covers both, matching every other "Contact"-labelled button in the file. Verified in preview: all three "Contact" links on the page resolve to `/contact`. _(AFK — one shared link object)_
- [x] **LR11 — `/contact` form is missing fields vs. the live form** _(done)_ → rebuilt the form-builder Contact Form to mirror the live Webflow form field-for-field (ground truth pulled from `migration/_extracted/contact-us.html`): First name + Last name (was one `full-name`), Email, Phone number, Title/Role, Organization, a required "How would you describe yourself?" select (Current Member / Prospective member / Organization partner / Press / journalist / Other, values verbatim so submissions bucket the same way ops already reports), and Message. Dropped the `subject` field the live form doesn't have. Name/contact rows are `width: 50` (two-up on desktop, one column on mobile). Also swapped the notification email's generic body for `{{*:table}}` so ops receives the full submission, not just a ping. Verified via the API (8 fields, select carries all 5 options) and in preview (all fields + dropdown render on `/contact`). _(HITL — product decision made: capture the full lead data to match live)_
- [ ] **LR12 — `/contact` Contact Details block missing `info@mapsnational.org`** → block schema already supports an email-icon item; just not added. _(AFK — one array item)_ **Deferred by decision:** left out on purpose — the rebuilt contact form (LR11) is the intended contact path, and the Contact Details block deliberately shows only the mailing address so submissions route through the form rather than untracked email. Revisit only if we want a public email address surfaced.
- [x] **LR13 — `/programs/private-sector-engagement` content parity verified** _(done)_ → the live source is at the typo'd slug `mapsnational.webflow.io/programs/public-sector-engagement` (the redirect map was right; my earlier "serves Policy Initiatives" note was stale). Diffed it against the local page: full parity. Hero (Empower / "MAPS Private Sector Engagement (PSE)"), the "Elevate" section heading, all three cards (GovCon Ecosystem Overview / Strategic Positioning for Firms / Teaming & Partnership Strategy), the "Grow" registration section, and the "Join MAPS as a PSE Member Here" CTA all match verbatim; the local hero paragraph, section intro, and registration body each carry one extra trailing sentence beyond the live copy (superset, nothing missing). Only deviation: the hero CTA reads "Email the PSE Team" locally vs. the live "Email our Private Sector Engagement Team" (40 chars) — the live label can't be used because link labels are capped at `maxLength: 35` in `src/fields/link.ts`, so the shortened label is the correct, validation-required form, not a regression. _(HITL — resolved: live source located at the typo'd slug, content confirmed faithful)_
- [x] **LR14 — SEO meta is empty on every single page** _(done)_ → `meta.title` / `meta.description` were `null` across all 35 pages. Backfilled per-page copy grounded in each page's own hero/section text: titles are concise and never restate "MAPS National" (`generateMeta` already appends " | MAPS National" to the title tag, so restating it would double it), descriptions kept ≤165 chars to avoid SERP truncation, no em/en dashes. Applied via one central `META_BY_SLUG` map merged in the shared upsert loop (`run()` in `scripts/seed-pages.ts`) rather than edited into every slice — the two hub one-liners (`programs`, `about-us`) and the `eventChild` factory (`events/*`) would each have needed a bespoke edit otherwise; the merge preserves any meta a slice sets itself (e.g. `meta.image`). Verified via the API: all 35 pages now report non-null `meta.title` and `meta.description`, zero missing. _(AFK — mechanical per-page title/description backfill, ~35 pages)_
- [x] **LR15 — `/programs/policy-initiatives` has `publishedAt: null` despite `_status: published`** _(done)_ → turned out much wider than described: 27 of the 35 pages had a null `publishedAt`, not just this one. Root cause: Payload only auto-stamps `publishedAt` on an actual draft→published *transition*, and the idempotent seed upsert (`run()` in `scripts/seed-pages.ts`) re-applies `_status: 'published'` to already-published docs every run, so a doc that was ever missing the stamp never got it backfilled. Fixed the shared upsert loop instead of the one page: whenever a page is published and has no `publishedAt` (on the incoming data or the existing doc), it's now stamped with the existing doc's `createdAt` (a plausible historical value) or the current time for brand-new docs. Verified via the API: all 35 pages now report a real ISO `publishedAt`, zero nulls remaining. _(AFK — data hygiene, fixed at the root instead of patching 27 individual pages)_

---

[#67]: https://github.com/MAPS-National/maps-website/issues/67
[#102]: https://github.com/MAPS-National/maps-website/issues/102
